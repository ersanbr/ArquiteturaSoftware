app.controller('HomeCtrl', function($rootScope, $location)
{
   $rootScope.activetab = $location.path();
});
 
app.controller('cadastraClienteCtrl', function($rootScope, $location)
{
   $rootScope.activetab = $location.path();
});
 
app.controller('cadastraPlanoCtrl', function($rootScope, $location)
{
   $rootScope.activetab = $location.path();
});